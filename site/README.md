# DMTA — site estático (FTP)

## Desenvolvimento
```bash
cd site
npm install
npm run build
npm run watch   # opcional, enquanto edita
```

## Upload FTP (produção)
Subir apenas o conteúdo gerado (ver README da resposta do agente). Não subir `node_modules/`.
